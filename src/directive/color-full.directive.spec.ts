import { ColorFullDirective } from './color-full.directive';

describe('ColorFullDirective', () => {
  it('should create an instance', () => {
    const directive = new ColorFullDirective();
    expect(directive).toBeTruthy();
  });
});
